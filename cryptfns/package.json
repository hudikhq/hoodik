{
  "name": "cryptfns",
  "version": "0.1.0",
  "scripts": {
    "wasm-pack": "wasm-pack build --out-name cryptfns --out-dir ../web/cryptfns --target nodejs --release",
    "wasm-pack-dev": "wasm-pack build --out-name cryptfns --out-dir ../web/cryptfns --target nodejs"
  }
}