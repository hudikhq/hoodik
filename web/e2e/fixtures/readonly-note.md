# Readonly note

This file is uploaded through the normal upload flow (not via the
"New File" button), so it lands as a regular file with `editable=false`.

Opening it should route to `/notes/:id`, but render in read-only mode
with a convert banner.
